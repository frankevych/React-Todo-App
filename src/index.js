import React from 'react';
import ReactDOM from 'react-dom';
//main app component that wrappers every component in src
import App from './App';

//rendering app component into  element #root
ReactDOM.render(<App />, document.getElementById('root'));
